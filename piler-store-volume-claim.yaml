apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: piler-store-volume-claim
  labels:
    app: piler
spec:
  storageClassName: piler-hynix-storage-class
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi