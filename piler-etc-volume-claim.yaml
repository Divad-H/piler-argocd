apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: piler-etc-volume-claim
  labels:
    app: piler
spec:
  storageClassName: piler-hynix-storage-class
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi