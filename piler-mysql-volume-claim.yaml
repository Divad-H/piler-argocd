apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: piler-mysql-volume-claim
  labels:
    app: piler-mysql
spec:
  storageClassName: piler-hynix-storage-class
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi