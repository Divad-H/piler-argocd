apiVersion: apps/v1
kind: Deployment
metadata:
  name: piler-manticore
spec:
  replicas: 1
  selector:
    matchLabels:
      app: piler-manticore
  template:
    metadata:
      labels:
        app: piler-manticore
    spec:
      initContainers:
        - name: conf-copy
          image: busybox:latest
          command:
          - cp
          - /etc/manticoresearch/manticore-src.conf
          - /etc/manticoresearch/manticore.conf
          volumeMounts:
            - name: manticore-config-volume
              mountPath: /etc/manticoresearch/manticore-src.conf
              subPath: manticore.conf
      containers:
        - name: piler-manticore
          image: manticoresearch/manticore:10.1.0
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: "256Mi"
            limits:
              memory: "512Mi"
          volumeMounts:
            - mountPath: /var/lib/manticore
              name: manticore-volume
          ports:
            - containerPort: 9306
              name: mysql-clients-port
            - containerPort: 9308
              name: http-clients-port
      volumes:
        - name: manticore-volume
          persistentVolumeClaim:
            claimName: piler-manticore-volume-claim
        - name: manticore-config-volume
          configMap:
            name: manticore-config
